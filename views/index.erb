<p>Your cards:</p>

<ul>
  <% @cards.each do |card| %>
  <li>
    <% if params[:flip] == card[:id] %>
    <%= card[:back] %>
    <% else %>
    <%= card[:left] %>
    <a href="/?flip=<%= card[:id] %>"><%= card[:middle] %></a>
    <%= card[:right] %>
    <% end %>
  </li>
  <% end %>
</ul>
